

## Plan: Match Quick Details Form Width to Blue Box

**File:** `src/pages/Checkout.tsx`

**Changes:**
1. **Line 476**: Remove `max-w-[520px] mx-auto` from the outer wrapper div so it spans full container width (matching the blue box)
2. **Line 483**: Remove `max-w-[480px]` from the inner div so the form content also fills the full width

