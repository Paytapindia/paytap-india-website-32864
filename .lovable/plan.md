

## Plan: Update Checkout Header Text

**File:** `src/pages/Checkout.tsx`

Change the header from:
> "Your Vehicle Payments Shouldn't Be This Complicated"

to two lines:
> "You're Losing Money on Every Vehicle"
> "You Just Can't See It"

Single text replacement — update the heading element to render the two lines with a `<br />` tag between them.

